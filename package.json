{
  "name": "build-tools",
  "version": "0.1.0",
  "main": "null",
  "scripts": {
    "lint": "for file in $(find nix -type f); do shellcheck --format=gcc $file; done;"
  },
  "repository": "https://github.com/electron/build-tools",
  "author": "Electron Authors",
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.4.1",
    "fs-extra": "^8.1.0",
    "yaml-js": "^0.2.3"
  },
  "devDependencies": {
    "shellcheck": "^0.3.0"
  }
}
